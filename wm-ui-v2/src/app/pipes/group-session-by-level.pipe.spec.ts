import { GroupSessionByLevelPipe } from './group-session-by-level.pipe';

describe('GroupSessionByLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupSessionByLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
