import { TopicfilterPipe } from './topicfilter.pipe';

describe('TopicfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TopicfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
